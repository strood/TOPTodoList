"use strict";

// Render logo box on given content
const renderLogo = (content) => {
  // Build a fixed size logo, and append to given content.
  content.innerHTML = "Scribbles";
}

export default renderLogo;
